require 'rails_helper'

RSpec.describe NfgUi::Components::Traits::Dismiss do
  let(:component_with_traits) { nil }
  let(:options) { {} }
  let(:traits) { [] }

  pending 'Dismiss trait spec is needed'

  describe 'registered traits' do
    subject { described_class::TRAITS }
  end
end
