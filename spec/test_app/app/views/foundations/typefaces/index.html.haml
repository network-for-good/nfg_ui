%h1 Typefaces

%h2 Without themes

Title:
= ui.nfg :typeface, title: 'Title'
%br
With Icon:
= ui.nfg :typeface, title: 'Title with icon', icon: 'rocket'

Heading:
= ui.nfg :typeface, heading: 'Heading'
%br
With Icon:
= ui.nfg :typeface, heading: 'Title with icon', icon: 'rocket'

Body:
= ui.nfg :typeface, body: 'Body'

%hr

%h2.mb-4 Traited versions of the typeface <b>(specifically for utilizing a block)</b>.
= ui.nfg :typeface, :title do 
  Title via do block
%br

With Icon:
= ui.nfg :typeface, :title, icon: 'rocket' do
  Title via do block with icon attribute

Heading:
= ui.nfg :typeface, :heading do
  Heading via do block
%br
With Icon:
= ui.nfg :typeface, :heading, icon: 'rocket' do
  Heading via do block with icon attribute
Body:
= ui.nfg :typeface, :body do
  Body via block

%hr

%h2 Tooltips on typefaces

Title:
= ui.nfg :typeface, title: 'Title', tooltip: 'Test Tooltip'
%br
With Icon:
= ui.nfg :typeface, title: 'Title with icon', icon: 'rocket', tooltip: 'Test Tooltip'

Heading:
= ui.nfg :typeface, heading: 'Heading', tooltip: 'Test Tooltip'
%br
With Icon:
= ui.nfg :typeface, heading: 'Title with icon', icon: 'rocket', tooltip: 'Test Tooltip'

Body:
= ui.nfg :typeface, body: 'Body', tooltip: 'Test Tooltip'

%hr

%h2.mb-4 Themes


= ui.nfg :typeface, body: 'Sample text without trait', theme: :primary, icon: 'rocket'
= ui.nfg :typeface, :primary, body: 'Sample text with trait', icon: 'rocket'

%hr

= ui.nfg :typeface, body: 'Sample text without trait', theme: :secondary, icon: 'rocket'
= ui.nfg :typeface, :secondary, body: 'Sample text with trait', icon: 'rocket'

%hr

= ui.nfg :typeface, body: 'Sample text without trait', theme: :success, icon: 'rocket'
= ui.nfg :typeface, :success, body: 'Sample text with trait', icon: 'rocket'

%hr

= ui.nfg :typeface, body: 'Sample text without trait', theme: :danger, icon: 'rocket'
= ui.nfg :typeface, :danger, body: 'Sample text with trait', icon: 'rocket'

%hr

= ui.nfg :typeface, body: 'Sample text without trait', theme: :warning, icon: 'rocket'
= ui.nfg :typeface, :warning, body: 'Sample text with trait', icon: 'rocket'

%hr

= ui.nfg :typeface, body: 'Sample text without trait', theme: :info, icon: 'rocket'
= ui.nfg :typeface, :info, body: 'Sample text with trait', icon: 'rocket'

%hr

.bg-dark.p-3
  = ui.nfg :typeface, body: 'Sample text without trait', theme: :light, icon: 'rocket'
  = ui.nfg :typeface, :light, body: 'Sample text with trait', icon: 'rocket'

%hr

= ui.nfg :typeface, body: 'Sample text without trait', theme: :dark, icon: 'rocket'
= ui.nfg :typeface, :dark, body: 'Sample text with trait', icon: 'rocket'
%hr
