- if collapse.heading
  = ui.nfg :button, :link, :block, collapse: "##{collapse.id}", body: collapse.heading, icon: 'caret-down', class: 'pl-0 text-left'

= render 'nfg_ui/bootstrap/collapse', collapse: collapse
